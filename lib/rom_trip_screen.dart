import 'package:flutter/material.dart';

class RomTrip extends StatefulWidget {
  const RomTrip({Key? key}) : super(key: key);

  @override
  State<RomTrip> createState() => _RomTripState();
}

class _RomTripState extends State<RomTrip> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(

    ));
  }
}
